/**
 * Deliberately empty.
 *
 * The template shipped nine identical "reviews" about *property management*
 * services, all signed "Arlene McCoy, CEO Themesflat", each with a stock
 * headshot. Autosouq has not traded yet, so it has no customers and therefore
 * no testimonials — and NICHE.md makes trust the entire proposition. Inventing
 * praise from people who do not exist is precisely the behaviour that makes
 * OpenSooq and Dubizzle untrustworthy at the affordable end.
 *
 * The five `Testimonials` sections that read this render nothing while it is
 * empty. Those sections should be removed from their pages until we have real,
 * attributable quotes from real buyers who agreed to be quoted; at that point
 * this array is where they go, in the existing shape:
 *
 *   { date, text, authorImage, authorName, authorTitle }
 */
export const testimonials = [];
